version: "2"
sql:
  - engine: "postgresql"
    queries: "./sql/queries"
    schema: "./sql/schemas"
    gen:
      go:
        package: "dbgen"
        out: "./internal/dbgen"
        sql_package: "pgx/v5"
        emit_interface: true
